# <模块名> 模块 API 契约（模板）

> **模板说明**：新模块文档 = 复制本文件为 `docs/src/<module>.md`，
> 全文替换 `<...>` 占位内容，删除不适用的章节，并与代码保持同步
> （改公共 API 必须同步更新本文档）。参考实例：`docs/src/platform.md`。

## 0. 使用指引

1. `cp docs/src/template.md docs/src/<module>.md`
2. 替换标题 `<模块名>` 与顶部 `<...>` 占位
3. 逐节填写：职责 → 引导流程 → API 面 → 命名框架 → 生命周期 → 变更记录
4. 无内容可填的章节整节删除（如模块无常量则删 3.3）
5. 每次修改 `src/<module>/` 的公共 API，必须同步更新本文档对应表格

## 1. 职责

<一两句话说明模块做什么>

边界：

- <不属于本模块的职责——防止职责膨胀>
- <与相邻模块的职责切分——如"不做 X，由 Y 模块负责">

## 2. 引导流程（如适用）

<文本流程图（代码块），标注调用时机与前置条件>；无引导时序的模块可整节删除。

## 3. 公共 API 面

### 3.1 函数

| API | 签名 | 语义与契约 |
|-----|------|-----------|
| `<name>` | `<pub fn name(args) -> Ret>` | <语义；注明契约：panic 条件、`unsafe` 前置、返回 `Option` 的降级语义> |

### 3.2 数据

<主要结构体；全 pub 字段时逐字段列表>

| 字段 | 类型 | 语义 |
|------|------|------|
| `<field>` | `<type>` | <语义> |

### 3.3 常量

| 名称 | 值 | 语义 |
|------|-----|------|
| `<CONST>` | `<value>` | <语义> |

## 4. 命名框架（对照 CLAUDE.md「API naming conventions」）

| 约定 | 本模块落点 |
|------|-----------|
| 读写对称（读方法 = 字段名，无 `get_` 前缀） | <示例：`get()`/`<field>()` 对应静态名或字段> |
| 不用缩写 | <列出本模块使用的术语全称与禁用缩写> |
| 查询 API 用查找动词，返回 `Option` | <示例：`find() -> Option<T>`；调用方决定降级> |
| 错误消息带 `&'static str` 上下文 | <示例：错误路径的消息形态> |
| 类型名与模块路径对齐 | <类型名与模块/路径的对应> |
| 全局只读态用 `OnceLock` | <示例：`static X: OnceLock<T>`，无 unsafe 读路径> |

> 只保留与本模块相关的行，无关约定可删。

## 5. 生命周期与并发

- <全局静态 / 锁选择（`OnceLock`/`SpinLock`/…）与理由>
- <`unsafe` 边界：哪些函数 `unsafe`、为什么（前置条件）>
- <长期持有引用的安全性论证（如 `'static` 引用来源与存活期）>

## 6. 变更记录

- `<日期>：<改动摘要>`（最新在顶）
